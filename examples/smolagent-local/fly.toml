app = "smolagent-local"
primary_region = "iad"

[build]

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  # Keep one machine warm — agentic loops are latency-sensitive; cold starts hurt.
  min_machines_running = 1
