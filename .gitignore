# macOS
.DS_Store

# IDE
.vscode/
.idea/
*.swp
*.swo

# Node
node_modules/
*.log
.env
.env.*
!.env.example

# Build artifacts
dist/
build/
out/
coverage/
*.tsbuildinfo

# Local tooling / agent state — not part of the project
.agents/
.claude/*
!.claude/skills/
.claude/skills/*
!.claude/skills/ogilvy/
skills-lock.json
.airlock-tasks.md
MEMORY.md

# Wrangler (Cloudflare) — when we start using it
.wrangler/

# Python
.venv/
__pycache__/
*.egg-info/
*.pyc

# Local LLM artifacts (large; not part of the project)
models/
tools/llama.cpp/

# Reference clone of the sibling airlock-config repo (read-only; not part of this repo)
/airlock-config/

# runtime state (per-project SQLite store)
**/.airlock/state.db
**/.airlock/*.db
python/agent-runtime/.venv312/

# harness live-test virtualenvs (Plan: prove all harnesses)
.venv-harness/
.venv-crewai/
