{
  "name": "airlock",
  "version": "0.2.0",
  "private": true,
  "description": "airlock — run any AI agent as a controlled, self-hosted HTTP service. Monorepo: CLI + dashboard (TS) + Python worker runtime.",
  "type": "module",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Okohedeki/airlock.git"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "biome lint packages examples package.json pnpm-workspace.yaml biome.json tsconfig.base.json",
    "format": "biome format --write packages examples package.json biome.json tsconfig.base.json",
    "check": "biome check packages examples package.json biome.json tsconfig.base.json"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.4",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "better-sqlite3"
    ]
  }
}
